# Sesión — 22 de abril 2026, 04:26–04:41 GMT+8

## Resumen

Fixes de base de datos y configuración final para que MejoraApp funcione en producción.

## Lo hecho

1. **Google OAuth configurado** — Provider activado en Supabase via Management API con Client ID + Secret de Google Cloud Console
2. **Columnas faltantes en profiles** — Agregadas `email`, `nombre`, `apellido`, `cargo` que faltaban del CLEAN_SETUP.sql original
3. **Login admin corregido** — Dos fixes:
   - Insertado `admin_username` = `admin` en `admin_config`
   - RLS policy cambiada a lectura pública (círculo vicioso: login admin necesitaba leer config antes de autenticar)
4. **Rol admin asignado** — User Pablo Eckert (`f387499e`) → rol `admin`
5. **Login con Google verificado** — Funciona, perfil se crea automáticamente, modal de completar perfil funciona

## Problemas encontrados

| Problema | Causa | Fix |
|----------|-------|-----|
| Perfil no se guarda | Columnas `cargo`, `email`, `nombre`, `apellido` no existían | ALTER TABLE ADD COLUMN |
| "El admin no está configurado" | Falta `admin_username` + RLS bloquea lectura pública | INSERT + DROP/CREATE POLICY |
| Google OAuth no funcionaba | Provider no activado en Supabase | PATCH config/auth API |

## Pendiente
- Verificar login admin en producción (usuario: `admin`, contraseña: `T@beg2301`)
- Rotar credenciales expuestas (GitHub token, FTP, Supabase token, Google Client Secret)
