

## Plan: Reducir tamaño del logo en pantalla de login

Cambio simple: en `src/pages/Auth.tsx` línea 89, reducir la clase del logo de `h-20` (80px) a `h-14` (56px).

### Cambio
- **Archivo**: `src/pages/Auth.tsx`
- **Línea 89**: Cambiar `className="h-20 object-contain"` → `className="h-14 object-contain"`

Tip: este tipo de cambios visuales los podés hacer gratis con **Visual Edits** (botón en el chat, abajo a la izquierda).

