# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Security — never commit credentials
.env
.env.local
.env.production
!.env.example
**/credentials*
**/secrets*
*.pem
*.key
dist.tar.gz

# Build artifacts
*.zip
*.tar.gz

# Lockfiles — keep only package-lock.json (npm)
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml

# Lovable internal
.lovable/

# Document files — use Documents/ folder, not root
/*.docx
supabase/.temp/
